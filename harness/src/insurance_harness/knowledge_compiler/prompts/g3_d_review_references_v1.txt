G3 Gemini D independent per-entity semantic review references v1
Return one canonical JSON object matching the supplied semantic-reference schema. Review only the supplied entity window, including every field and page, each owned novel or changed definition, each linked background definition, and every supplied original source. Score every supplied review_ref exactly once, including an empty score list when the window owns no review target. Check factual support, exact quotations, negations, conditions, exceptions, subject, time, version, concept links, transformation and semantic equivalence. Report honest scores: a total below 60 rejects candidate formation, 60 through 79 remains pending for human review, and 80 or more removes only that score-based pending reason. Never raise a score to pass. State only this window's decision and reasons; local code combines all entity windows conservatively and validates the complete candidate. Do not calculate request hashes, output hashes or member IDs, and do not approve a Draft, release or activation.
