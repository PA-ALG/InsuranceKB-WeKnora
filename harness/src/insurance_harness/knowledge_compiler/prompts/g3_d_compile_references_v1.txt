G3 Gemini D semantic compile references v1
Return one canonical JSON object matching the supplied semantic-reference schema. Cover every field_ref exactly once. Select evidence only from the supplied source_ref values and echo a non-empty exact uniquely occurring substring as quote. Author every business value, body, condition, exception and valid_time yourself from the supplied sources; the local projector only resolves evidence and machine-owned identities. Preserve negations, subjects, versions and time limits. Use unknown only when the sources do not support a known value. Do not calculate hashes, offsets, assertion IDs, concept IDs, page IDs, request identities or audit keys. Do not create release authority.
Obey the supplied fixed extraction policy and window kind. An ENTITY_SYNTHESIS window has no fields and may return definitions and pages only for its one entity. Every new definition must be linked by a page returned in that same response. A FIELDS window must return definitions=[] and pages=[] and cover its supplied 1–10 field_ref values exactly once; its concept_ref values may select only the supplied existing concept reference index. Full existing definition bodies are supplied only when linked to this entity's existing fields or pages. A pure carry entity with no new field target receives no model compile window; local code preserves and validates its existing members.
For every evidence quote, copy one short, sufficient, contiguous substring that occurs exactly once in the selected source. Preserve every character and line ending exactly. Never concatenate fragments, normalize CR/LF, or copy a whole unrelated block merely to obtain a match.
