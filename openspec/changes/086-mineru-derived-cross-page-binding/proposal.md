# 086 · 596-1 MinerU Derived Cross-page Binding

## Goal

Determine whether the exact 596-1 MinerU rate-table intake can support one
honest, replayable `DERIVED_STRUCTURAL_RELATION` between real 053 table
endpoints. Current 062-only input remains `NOT_AVAILABLE`: MinerU 3.4.4
observations do not distinguish `cross_page` from `lines_deleted`. A minimal
future-089 replay Protocol may close that exact marker-kind/path custody and
enable a derived—not native—binding without changing the parser contracts.

## Scope

- consume only an exact 083 three-source intake and matching 053/060 rate
  `ParsedDocument` plus `ParseManifest`;
- prove that adjacent pages, one native observation, compatible grids/spans,
  identical mechanical header-cell digests and one unique candidate pair are
  still insufficient without a typed native marker identity;
- accept no caller-supplied marker; consume only a closed future-089 replay
  authority that binds the exact 083 observation to marker kind, structural
  path and actual 053 endpoint IDs/pages;
- generate table bindings only for one unique mechanically compatible pair,
  and section bindings only for exact typed block endpoints on different pages;
- expose one immutable task-local DTO and deterministic replay validator for a
  later 084 protocol adapter;
- report section binding as `NOT_AVAILABLE` while explicit block facts cannot
  prove equally unique endpoints.

## Non-goals

No native-relation claim, ADMIT/READY decision, Markdown/body inference,
LLM/VLM/provider, Golden, database, WeKnora, parser platform, 083/084/085 or
future-089 implementation, modification or production persistence is included.

## Path ownership

Exactly seven paths: this registry row, four OpenSpec files, one task-local
module and one focused test. The registry ownership is insertion of the `086`
row only; all other task rows and placeholder semantics remain untouched.
