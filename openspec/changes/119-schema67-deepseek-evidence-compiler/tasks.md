# 119 Implementation Tasks

- [x] T1 Freeze exact main, workbook authority, path budget and clean baseline.
- [x] T2 Define the parent OpenSpec and the Lane B/C public boundary.
- [x] T3 Write focused REDs for exact Schema67 identity, tri-state semantics and deterministic hardness.
- [x] T4 Implement immutable schema contracts and hardness with canonical hashes.
- [x] T5 Write REDs for exact profile/template selection and no-schema-only fallback.
- [x] T6 Implement the minimal selector and GenericFactEnvelope custody.
- [x] T7 Run focused and bounded regression, Ruff, strict mypy and OpenSpec strict.
- [x] T8 Run candidate diff/scope/privacy gates and freeze one exact temp-index tree.
- [x] T9 Correct product/review-package identity, exact ordered67 authority,
  production/adjudication separation and role-subset task selection with REDs.
- [x] T10 Bind exact workbook A5:H71 row content and approved current-material
  routing; reject caller-rehashed schema mutations and synthetic Schema67 rows.
- [x] T11 Bind the exact approval authority reference in the snapshot and
  contract-set custody; reject caller-rehashed suffix substitution.
- [x] T12 Bind exact official DeepSeek authority: provider `deepseek`, base URL
  `https://api.deepseek.com/v1`, model `deepseek-v4-flash`, request-level
  `thinking={"type":"disabled"}`, no `enable_thinking` field,
  eight-task/21-deferred plan, strict JSON, shared two-extra retry/repair budget and
  production transport boundary.
- [x] T13 Bind linyao expert receipt, exact67 candidate membership and 057
  Evidence replay without claiming semantic equality.
- [x] T14 Replay the three real admitted MinerU artifacts without provider;
  recover only exact native block-hash preimages, narrow locators from contract
  text, prohibit caller-selected locators, enforce the exact HTTP-envelope
  128 KiB bound and keep Evidence-only success non-publishable.
- [x] T15 Replace the redundant LLM Locator stage with code-owned deterministic
  FieldContract plus MinerU selection; bind policy/authority/selection hashes,
  record zero Locator calls and cap the exact eight-task batch at ten provider
  calls with at most two extras across retry, response-contract repair and
  Evidence repair.
- [x] T15a Restrict model output to semantic fields and locator-ref/quote pairs;
  hydrate all custody in code, reject cross-field refs before 057, keep
  rate-table fields unknown until exact table/cell recovery, and prove the
  orchestrated exact-10/11th-pretransport budget boundary.
- [x] T15b Remove the legacy single-task public transport bypass and bind the
  locator policy to exact casefold normalization plus whole-CJK sequence bounds.
- [x] T15c After the first bounded real batch failed closed on its first
  Extractor response, reproduce the hidden Prompt/validator contract as a RED;
  expose the existing exact keys/order, tri-state invariants, forced-unknown
  fields and field-local normalized-replayable Evidence rules without relaxing
  validation.
- [x] T15d Expose the existing complete parser boundary for `field_id`,
  `value_snapshot`, `locator_ref` and `quote_snapshot`: length 1 through 512,
  single-line, no CR/LF and no leading/trailing whitespace; prove the full
  visible constraint set matches parsing without relaxing hydration, locator or
  057 gates.
- [x] T15e Classify Extractor failures with fixed secret-free reason codes and
  allow one response-contract regeneration only for model-correctable shape,
  ordering, forced-unknown, string or field-local locator errors. Bind failed
  and accepted response hashes without persisting content, consume the shared
  two-extra budget, and keep code-owned custody failures immediately
  fail-closed. Keep the one identical empty/invalid-JSON retry distinct from
  response-contract and Evidence repair while retaining the exact ten-call
  batch ceiling and rejecting a third extra pretransport.
- [x] T15f Route an initial Extractor response that remains empty or invalid
  JSON after its exact identical retry through the same single response-contract
  response-contract repair kind. Bind the fixed decode category and
  second-response digest without
  retaining raw content; reuse the same parser, hydration and 057 gates, forbid
  a fourth call after regeneration failure, and preserve the full `8+1+1=10`
  batch ceiling; Evidence repair may coexist only when it is the second and
  final extra. Require decode
  repair receipts to bind the exact `2 extractor + 1 repair + 1 retry` history,
  rejecting a rehashed shorter generic-budget tuple.
- [x] T15g Read the exact approved 596-2 workbook once under total-control,
  freeze the ordered-67 hash-only reference authority (45 present, one explicit
  absence, 21 unknown and 94 Evidence branches), and expose only a code-owned
  deterministic comparator. Reject caller-selected reference rows or fake
  comparator authorities even when their local hashes are recomputed; unknown
  remains pending and any semantic or Evidence failure keeps Wiki admission
  false with zero publishable fields.
- [x] T15j Seal the one pre-approved linyao receipt issue/expiry/provenance/hash
  identity and require the comparator authority to join the exact base reference
  bundle, subject and receipt before any field comparison.
- [x] T15k Add the minimal B/C rescue on integrated tree
  `6d160c276efd77f1e067430325e7159724fd58fa`: make the first Extractor
  request expose normalized present-value/quote equality, required-role
  coverage, field-local slots, replayable quotes and unchanged forced unknowns
  without catalog duplication or Golden/reference content. Demote the complete
  code-owned VerificationBatch non-PASS union in prepared field order, with any
  non-PASS multi-source branch demoting the whole field; preserve PASS output
  and `FreeformEvidenceBindingReceiptV1` bytes. Keep all structural/parser/
  envelope/string/locator/hydration/source/verifier failures terminal and set
  retry plus both repairs to zero. Bind one private
  `EvidenceDemotionReceiptV1` with exactly its nine non-budget fields; require
  the loader to recompute exact scope and PASS preservation. Emit demoted unknown
  as PENDING + ReviewItem
  `EVIDENCE_NONPASS_DEMOTED`, skip unknown comparison and keep Wiki false/
  publishable zero. Bind prior `2`, current task/provider/extractor `8`/`8`/`8`,
  zero locator/retry/repair calls and cumulative `10` only in existing
  `Schema67BatchExecutionReceiptV1` and `Schema67BudgetReportV1`; add no budget
  field to the demotion receipt, durable coordination, restart facility, child
  OpenSpec or generic platform.

  Exact TDD matrix: every numbered row MUST first be run and observed RED, then
  implemented and observed GREEN.

  1. Parameterize first-request prompt validation over normalized present-value/
     quote equality, required-role coverage, exact field-local slot membership,
     quote replay, forced-unknown constraints, a single catalog copy and no
     Golden/reference content; each omitted or mutated rule fails before transport.
  2. One eight-field batch with seven PASS and one non-PASS demotes exactly one.
  3. Any non-PASS branch demotes one whole multi-source field.
  4. Forced-unknown fields remain unchanged without entering demotion scope.
  5. Loader rejects missing, extra, duplicated, reordered, replaced or self-rehashed
     demotion scope.
  6. Loader rejects mutation of any non-demoted PASS output or preserved
     `FreeformEvidenceBindingReceiptV1`.
  7. Structural, parser, envelope, visible-string, locator, hydration,
     source-custody or verifier error yields zero Candidate and no demotion.
  8. A trusted-loader round trip accepts `Schema67BatchExecutionReceiptV1` only
     with `prior_provider_calls=2`, existing `task_count=8`, `provider_calls=8`,
     `extractor_calls=8`, `locator_calls=0`, `transport_retries=0`,
     `response_contract_repairs=0`, `evidence_repairs=0`, `repair_calls=0` and
     `cumulative_provider_calls=10`; parameterized one-at-a-time drift of each
     fact is rejected even after recomputing `batch_receipt_sha256`.
     `Schema67BudgetReportV1` mirrors those exact facts, while any budget-field
     injection into exact-nine `EvidenceDemotionReceiptV1` is rejected even
     after recomputing `receipt_hash`.
  9. All eight real serialized request bodies remain below `131072` bytes.
  10. Lane C/report maps demoted output to PENDING + ReviewItem, skips unknown in
      the comparator, keeps Wiki false/publishable zero and leaks no forbidden
      demotion content.

  This matrix SHALL keep `EvidenceDemotionReceiptV1` to exactly
  `policy_sha256`, `parent_bound_attempt_sha256`, `verification_batch_hashes`,
  `demoted_field_ids`, `initial_output_sha256`, `final_output_sha256`,
  `final_evidence_receipt_hashes`, `pass_preservation_sha256` and `receipt_hash`;
  its private/public projections SHALL contain no pre-demotion value, quote,
  locator/ref, slot, request/response or filesystem path. Budget facts SHALL
  remain separate in existing `Schema67BatchExecutionReceiptV1` and
  `Schema67BudgetReportV1` only.

  Provider-zero closeout:

  - [x] Complete the minimal B+C implementation, observe the exact-10 matrix
    RED then GREEN, and obtain independent final Spec review GO with zero
    blockers.
  - [x] Record fresh DeepSeek `69 passed`, Expert `77 passed`, Report `35
    passed` and root bounded `279 passed`; Ruff lint, strict mypy, strict
    OpenSpec and diff-check are PASS.
  - The two-file DeepSeek format-check is not claimed as PASS: the production
    module and focused test report historical whole-file formatting differences,
    and no formatting rewrite was performed.
  - At this provider-zero closeout checkpoint, provider, WeKnora, DB,
    credential, Excel and Golden actions were all `0`; Candidate publication,
    evaluator execution and real Wiki execution were NOT RUN. The later T16
    terminal below supersedes only the provider-execution portion.
  - [x] T15m Correct the successor declaration to provider `deepseek`, official base
    URL `https://api.deepseek.com/v1`, exact model `deepseek-v4-flash`,
    request-level `thinking={"type":"disabled"}`, no `enable_thinking` field
    and JSON-object response format. Explicitly reject `aliyun`, DashScope and
    `deepseek-v4-flash-0731`; require the exact tenant/Space/unique active
    `KnowledgeQA` model-row non-secret identity before credential access, with
    row source `remote`, runtime provider `deepseek` and execution provider
    `deepseek`.
    Preserve the v9 calls made under the old `aliyun` provider label only as
    `prior_provider_calls=2` cross-identity governance-budget history, never as
    new response semantics, model-receipt or Candidate ancestry; keep current
    exact eight and lifetime exact ten. Do not claim a model-quality
    improvement. This specification corrective was completed provider-zero.
- [x] T16 After total-control verification and explicit user authorization, the
  exact8 official-DeepSeek batch was executed exactly once. It terminated
  `EXTRACTOR_RESPONSE_INVALID`; Candidate remained absent and no offline
  Candidate semantic/Evidence scoring ran. The failed identity is frozen and
  MUST NOT be rerun.
