# 097 · 596-1 No-provider Full Synthetic Vertical Rehearsal

## Goal

Freeze one task-local, provider-free compatibility rehearsal for the pending 596-1
capture-to-admission stack. The rehearsal uses structurally realistic synthetic
terms, brochure and rate-table artifacts and proves the exact dependency sequence
`091 custody → 096 relation receipt → 095/087 wiring → 094 wrapper` without claiming
real MinerU admission, Golden quality or Release readiness.

## Scope

- Invoke one injected fake capture executor, then each dependency port exactly once
  in fixed `terms → brochure → rate_table` order.
- Recompute every supplied preimage SHA-256 and carry custody, relation manifest,
  relation decision, wiring and wrapper receipt identities through one safe chain.
- Preserve the current single-endpoint terminal
  `BLOCKED_ON_CROSS_PAGE_BINDING`; a separate complete-endpoint fixture proves only
  that later public DTO adapters can become mechanically executable.
- Pass every dependency fail-closed reason through without retry, fallback, partial
  success or external action.

## Non-goals

No real capture, credential, provider/model, Golden, DB, PostgreSQL, WeKnora, live,
full, ADMIT, READY or Release action. No relation inference from paths, adjacent pages
or Markdown. No duplicate implementation of 091/096/095/087/094 and no generic
workflow or compatibility platform.
