# 083 · 596-1 MinerU Capture Custody Intake

## Goal

Admit only the exact bytes emitted by the existing Go
`mineru-semantic-content-custody.v2` capture contract into an immutable,
deterministic terms → brochure → rate bundle. This boundary proves capture custody; it
does not prove parsed-document quality or publication authority.

## Scope

- accept three in-memory byte strings and no filesystem path;
- validate the closed Go JSON shape, exact 596-1 source order, parser/attempt/call/status
  identities and every content, structure, cross-page and capture digest;
- retain the validated same-read snapshots in immutable DTOs while excluding sensitive
  snapshots from representation and ordinary serialization;
- emit one canonical bundle digest over the exact three admitted capture identities.

## Non-goals

No `ParsedDocument`, `ParseManifest`, ADMIT decision, Golden, model, provider, credential,
filesystem, database, WeKnora, migration, network or generic parser platform is included.

## Path ownership

Exactly seven paths: this registry row, four OpenSpec files, one task-local production
module and one focused test. The registry ownership is insertion of the `083` row only; the
existing `082+` placeholder and any stacked `084` semantics remain byte-untouched.
