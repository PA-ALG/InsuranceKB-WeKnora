# 061 · Validation Report

## Identity

- Base/HEAD: `e388e8326ab20e134b82d532799b15cfc18e2387`
- Base tree: `6bdf418c206c673b7391130fd2ebf36c02b3e804`
- Branch: `codex/061-596-1-vertical-falsification`
- Merged dependencies: 060 PR #88 and 059 PR #89
- Scope: six paths; README registry intentionally unchanged
- State: `DETERMINISTIC SCORER GREEN / 3 PARSE ARTIFACTS REQUIRED`
- Mainline observation: `origin/main` advanced to `80fa4fbb...` through PR #90's
  non-overlapping four-path Go capture change. This candidate remains bound to the
  authorized `e388e832...` base for exact-tree review; no functional rebase was attempted.

## Authority and dependency facts

- 060 public module `insurance_harness.compiler.native_mineru_cloud` is present,
  including its native builder and merged 053 ParsedDocument/manifest/quality types.
- 059 named-human activation authority is Go, not Python:
  `HumanBatchDecisionReceiptV1`, `ActivateReviewed` / `Revert`, the HTTP handler,
  immutable Head receipt and opaque pinned reads.
- `insurance_harness.knowledge_compiler.candidate_releases` does not exist and is
  not an 061 dependency. The scorer does not reproduce a local Release DTO,
  signature verifier or authority adapter.
- Admission requires the real 060 seam plus exactly three immutable typed receipts
  binding exact role/source/profile, sanitized bytes, raw/sanitized hashes and approved
  052 resolution. It replays the real 060 builder and requires exact equality of the
  recomputed ParsedDocument/manifest/terminal ADMIT decision. Until then it reports typed
  `BLOCKED_ON_REQUIRED_CONTRACTS`, `provider_calls=0`, `golden_reads=0`.
- The 060 module is resolved lazily inside admission. Missing/broken imports produce the
  same typed zero-I/O block instead of preventing 061 import. Nested 052/053/060 DTOs are
  revalidated before dereference/replay; malformed `model_construct` objects fail closed
  without propagating raw exception detail. Required 060 exports are captured once in
  that boundary; downstream code performs no second module attribute lookup.
- Quality success is `QUALITY_GATES_PASS_PENDING_HUMAN_RELEASE`. Final
  `MVP_VERTICAL_SLICE_GO` remains a total-control result after the real Go 059
  activation endpoint returns immutable receipt/Head and pinned read/revert are
  demonstrated.

## RED / GREEN evidence

- Cross-language authority RED: three focused tests failed because the old code
  required two Python public contracts, remained blocked after three admitted
  artifacts, and returned `MVP_VERTICAL_SLICE_GO` directly.
- GREEN removes only the nonexistent Python 059 oracle. A fake Python 059 module
  cannot affect admission; exact 060 plus three artifacts opens only
  `READY_FOR_QUALITY_FALSIFICATION`; the scorer exposes no Release/Head/receipt
  input and can return only pending-human-release or typed NO-GO.
- Evidence-boundary RED reproduced exact 57/60 known Evidence being rejected;
  GREEN rejects only values below 95 percent, so exact 95 percent is admitted.
- The critical18 test independently reconstructs the canonical preimage in exact
  `[contract, schema_version, source, fields]` order and matches both the ordered
  tuple and approved canonical SHA256.
- VF1 custody RED first reproduced three arbitrary SHA claims opening readiness, then
  reproduced hand-built matching manifest/decision hashes with
  `parse_policy_receipt=None` opening readiness. GREEN requires replay inputs and the
  approved exact 052 catalog/profile capability authority, invokes the real 060 builder,
  and rejects any byte, identity, resolution, object, hash or terminal-decision drift.
- Budget ledger enforces baseline 6, candidate main 8, repair 4, total 18 and
  rejects fallback/retry.
- Frozen arm envelopes bind exact product/three sources/Schema/parser/model/prompt/
  budget/normalizer/comparator identities, approved arm-profile SHA256
  `c64ce6227b714fb9a47fe2c15cd51349df4fccc8770fb95442aed86061f39fe3`,
  DeepSeek semantic identity and distinct approved parser roles.
- Self-consistent product/source/Schema/model substitutions are rejected against exact
  ProductVersion `596-1`, ordered PDF source identities, Schema
  `v1.1+b31a411c621c`/registry SHA256
  `5d222c68f228d57c9061fc329f85a26191f6c847f7122f221e6aff92147b9db5` and the
  canonical identity of the approved DeepSeek model facts.
- The critical18 test owns an independent hard-coded priority/id/name preimage and does
  not read the production `APPROVED_CRITICAL18_FIELDS` tuple as its oracle.
- The scorer binds the approved ordered critical18 tuple and SHA256
  `12b648d509c53b7ce1659abbf95811d437c3d22f729d46a58545f47e09bee344`.
- Security/Data RED reproduced three fail-open boundaries: exception-raising 060 symbol
  access leaked raw detail; a foreign noncritical field could replace an approved
  Schema60 field; and both arms could jointly mutate model/prompt/budget/normalizer/
  comparator components while retaining the approved profile label. GREEN encloses
  import plus all symbol reads in one no-detail boundary, requires the approved exact
  ordered Schema60, and recomputes the approved profile/component identities.
- Delivery RED reproduced caller rate declassification and Golden surface-only custody.
  GREEN derives rate semantics from exact Schema IDs `zh_7fe8603c08` and
  `zh_c588207763`, binds the exact 049 release/artifact/approval subject and ordered
  sources, and recomputes a deterministic score receipt over both arm hashes, Golden
  custody, evaluator, budget, metrics and reasons.
- Quality RED reproduced caller-injected Golden fields remaining detached from the
  claimed artifact hash and direct scoring without parse admission. GREEN makes the
  public scorer replay exact three-artifact admission first, binds the recomputed
  admission digest into both arm outputs, then accepts only exact approved `596.jsonl`
  bytes (SHA256 `562c37c7...d8fb`) and strictly derives the internal Golden. Non-READY
  admission reads zero Golden bytes.
- Security RED reproduced a two-read module whose second `__getattr__` leaked secret and
  private-path detail. GREEN captures the full 060 export set once in an immutable
  object and never reads module attributes downstream.
- Budget mutation RED covers `bool`, float and NaN; only non-negative runtime integers
  are admitted.
- Mutations cover hash drift, critical identity substitution/order, parser role,
  exact60 foreign/missing/extra/duplicate IDs, arm-profile component drift, Golden
  release/artifact/approval drift, caller rate declassification, tri-state/value/
  hallucination/Evidence thresholds, invalid rate locators and budget overflow. Golden
  is strictly derived from exact approved bytes only after admission and frozen arm
  hashes.

## Current gates

- focused 061 test: `107 passed`;
- bounded 059+060+061 tests: `136 passed`;
- Ruff on the two Python paths: `PASS`;
- strict mypy on the two Python paths: `PASS`;
- OpenSpec 061 strict: `PASS` (PostHog DNS flush noise after validation; exit zero);
- diff/scope/privacy/credential scan: `PASS`; the successor exact candidate identity is
  reported externally after the six-path temp index is frozen.

## NOT RUN

Provider, model, live, database, PostgreSQL, WeKnora, full suite, commit, push, PR,
Ready, merge. Focused deterministic tests did read exact approved `596.jsonl` bytes to
verify file-hash/strict-parser custody; no Golden value entered a prompt, repair choice
or provider execution.

## 2026-08-02 post-merge capture-policy corrective

- A bounded real capture on execution base `34f011d17bf575c2cfd089585e7c1f68efb19ac8`
  used capture-library blobs identical to main `14ede0860785fbb5a0cd36c539fcc6335b04ca58`.
  Credential admission succeeded without persistence or output. Terms reached one allocation,
  one upload and twenty status polls, then stopped before ZIP download; brochure and rate made
  zero calls. No evidence artifact was published and no automatic retry or fallback occurred.
- Root cause: `minerUCaptureTimeout=9m30s` and `defaultPollInterval=3s` allowed 190 bounded polls,
  while the independent fixed cap was 20 (about 57 seconds of waiting after the first poll).
- Corrective RED failed with `got=20 want=190`. GREEN derives the fixed bounded poll cap from
  `minerUCaptureTimeout/defaultPollInterval`; transport errors remain fail-fast, production sleep
  remains three seconds, redirects remain zero, and retry/fallback behavior is unchanged.
- Affected MinerU tests and `go vet ./internal/infrastructure/docparser`: `PASS`.
- Corrected provider capture, commit, push, PR, Ready and merge: `NOT RUN`.
