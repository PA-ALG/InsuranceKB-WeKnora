# G3 PR #130 CI 收口（2026-09-22）

本轮仅收口已授权的 G3 PR #130，复用现有工作树与 OpenSpec 129；未部署、未迁移数据库、未调用模型或重新处理业务材料。真实网页 FLOW 结论仍以 [平台验收](task3bn-platform-closeout-20260922.md) 为准。

## 范围与原始失败

基线提交 `8ad4eac138078ec794448320cc0f19a5c64479cb`。首次远端 [CI 35707328803](https://github.com/PA-ALG/InsuranceKB-WeKnora/actions/runs/35707328803) 为 Ruff 57 项和跨测试模块导入失败；本地 strict mypy 进一步发现 3659 项错误（147 文件）。未调整检查配置、跳过规则或移除测试来通过门禁。

修复沿用 G3-AUTO-1—6 / G3-DISC-1—2：补齐既有窗口、路由、任务、存储、HTTP 和测试接口类型；修复测试导入；兼容省略 issuer_aliases 的旧策略在 Pydantic 重验证时被误拒绝的问题，显式空声明仍拒绝。

集中检查拦截并在提交前关闭了类型整理产生的回归：恢复与分类回执的字段顺序变化（保持旧 wire/hash）、运行期缺失 ModelIdentity 导入、JSON 行被过早转换为模型对象导致候选传输失败，以及适配器新增 kind 检查破坏原调用边界（恢复由下游 projector 校验完整窗口）。另恢复测试 fixture 的实例状态，旧回放测试显式使用既有 legacy 入口；当前 checkpoint 公共恢复路径单独验证。未减少原断言。

## 本地验证

冻结实现 tree：`326aa8f640c3035a79abdda77a34542eb9ce903b`；验证记录的后续变更不改变该实现。

| Requirement / 不变量 | 实现与测试 | 状态 |
| --- | --- | --- |
| G3 CI 静态门禁 | `ruff check .`；`mypy src tests`（668 文件）；`git diff --check` | PASS |
| 原策略与持久化回执兼容 | issuer alias 省略/显式空；recovery v1/v2/v3 固定 bytes/digest；classification V2 固定 wire SHA-256 | PASS |
| G3-AUTO-3/6：候选无损传输与编译 | `test_candidate_transfer.py` + `test_compilation.py`，24 passed / 111.16s | PASS |
| G3-AUTO-3/4/6：身份重试、旧回放 | 当前 worker 身份重试 + `test_identity_recovery.py`，31 passed / 117.88s | PASS |
| G3-AUTO-3/4：检查点重基线 | legacy/rebase 8 passed / 356.59s；指定恢复窗口 6 passed / 9.70s | PASS |
| G3 有界调用与重用 | compiler lane bounded/review/issuer 84 passed；recovery/projection 12 passed；分组存在重叠，不累加为总数 | PASS |
| 完整确定性测试 | 本地分组继续执行；最终由 exact commit 的 GitHub CI 记录，不从上述有界结果推导 | NOT RUN（尚未完成） |
| PostgreSQL 集成 | 由 GitHub 隔离 PostgreSQL service 执行；不使用现有业务数据库跑破坏性 fixture | NOT RUN |

首轮宽域诊断保留失败结论：15 failed / 94 passed（旧 issuer 策略）；10 failed / 333 passed（wire 与测试 fixture）；修复中已载入旧 helper 的进程为 11 failed / 91 passed 后终止，不能当作最终实现结果。最后实现按冻结代码重新验证。

## 独立审查与交付维度

独立只读 reviewer `ci_frozen_review` 已基于上述不可变 tree 复核本轮 CI delta，结论为 BLOCKER 0 / BACKLOG 0 / REJECTED 0；曾复现的 adapter kind 检查问题已在最终 tree 关闭。Reviewer 确认本验证记录可作机械文档豁免，不修改验收标准。PR 保持 Draft，不声称已获整批 G3 合并批准。

- software：本地静态及有界回归 PASS；完整测试单独记账。
- container health / provider probe / provisioning / local live：本轮 NOT RUN，effect=0。
- GitHub live：首次失败保留；本轮推送后以精确提交关联 CI 为准，当前 NOT RUN。
- QUALITY、规模长稳、生产部署仍不在本轮结论内。

## 第二批完整扫描与集中修复

第一批已提交并推送 `3f3a4cadeef56e6186ad6839ff34ef8a38be608a`。远端PR run `35717308908` 的Ruff、strict mypy、wheel-smoke通过；PostgreSQL真实执行为6失败/26通过/28 errors，根因为迁移head与旧测试fixture不一致。本地完整扫描暴露的结果保留：非ingestion为11失败/6609通过/17跳过/65 deselected；ingestion为2失败/572通过/10 fixture errors。不得将这些历史诊断写成最终通过。

第二批按同一Goal集中处理：

- 迁移head测试对齐实际单链0018，0015专项测试仍明确测试0015边界，不删除危险降级断言。
- 原scope6失败揭示真实安全缺口：0018/17/16先DDL后才到0015检查。现在由命令起点先解析完整降级计划，委托既有0015/0006等数据检查；相对目标只解析一次，未跨越的领地不误查，offline跨保护域在DROP之前拒绝。没有执行业务数据库降级。
- 旧恢复fixture显式走历史入口，当前workflow精确断言checkpoint v6；保持原恢复反例。
- MVP权限架构测试区分只读ModelIdentity.provider元数据与执行边界，新增AST调用/导入/赋值反例，未新增模块排除。
- 独立发现generation v4以本地依赖作稳定ref，移除整批请求编号依赖；保留完整SourceBlock、实体身份/版本/Schema/Profile/证据依赖和完整ConceptDefinition。首次CREATE→MATCH的五项接入回执不属于生成输入，仍留在compile_request。v3历史context按旧版读取/投影，冻结HEAD与新代码的v3全fixture context逐字一致。
- v4父响应按当前请求严格重投影，原raw保持；额外校验原请求字节/hash和model/prompt policy。模型策略变化不复用；请求证据损坏及未知发送结果不允许重发。复用聚合summary保留真实parent run。

第二批源码冻结审查tree：`a6327ef6db4da76a77096fa25c8588f7ec36033a`（本记录的后续验证事实不改变源码）。

| Requirement / 检查 | 实际结果 |
| --- | --- |
| 静态检查 | Ruff全仓库PASS；strict mypy 669文件PASS；diff-check PASS |
| G3-AUTO-5 迁移及权限架构 | 131 passed / 49.83s；临时SQLite、provider-zero |
| G3-DISC-1 / G3-AUTO-3 发现依赖/投影/custody/metrics | 57 passed / 35.51s；后补CREATE→MATCH、worker与聚合summary完整验证单独记录 |
| 历史v3 context兼容 | 冻结HEAD旧renderer与当前显式v3所有fixture实体context字节相同，PASS |
| 当前完整ingestion回归 | 首次603项：600 PASS/3失败；缺请求custody与Alembic环境的两处fixture已修。另一次旧recovery RUNNING在单例及整组复验均通过，未猜测修改生产实现；最终完整结果等待最新commit CI |
| 最新源码远端CI与PG集成 | NOT RUN（待第二批提交推送） |

非空发现已发布后，exclusion index新增成员是实际输入变化；本次不忽略该变化强复用旧“新成员”响应，也不将EMPTY或输入完全不变的回归扩大成所有自由发现增量策略已闭合。全新产品真实网页验收仍引用既有部署身份，本轮没有部署、provider、原文上传、Candidate或发布effect。

本地wheel的单项失败为uv0.9.26在macOS SystemConfiguration初始化时panic，build backend尚未启动；远端Ubuntu wheel-smoke通过。未添加skip或削弱wheel断言。完整GitHub确定性检查仍以第二批精确commit为准。

第二批收口复验：当前workflow3完整首跑/重启/单字段补抽fixture **1 passed / 52.30s**，确认来源不重抓、只新增失败字段调用、自由发现生成仍累计1次、正常发布及summary父run保持。恢复/source stages/发现预算/workflow迁移/MVP权限架构整组 **102 passed / 52.76s**。全量静态重新执行Ruff和strict mypy，结果单列最终提交核验。原3失败及未完成的全量重跑不抹除；最新提交CI在推送后观察。

最终复审对象为tree `aebf7ab5e7fa509cb17008531cc3ee225e6bca30`（相对a632只测试fixture、AST guard修正及验证记录）；源码行为没有额外变更。后补本段只更新真实验证事实，不变更Requirement或历史FLOW。

最终独立复审 `g3_submission_review`：基于 `aebf7ab5e7fa509cb17008531cc3ee225e6bca30` 的最后delta，BLOCKER 0 / BACKLOG 0 / REJECTED 0。审查者从冻结tree独立导出验证：discovery budget 8 PASS、workflow version 7 PASS、MVP AST/架构11 PASS，三文件Ruff/strict mypy及diff-check PASS。前轮源码审查在a632无源码阻断，唯一AST guard阻断已关闭。root最新 `ruff check .` PASS；`mypy src tests` 669文件PASS。本段为已认可的机械证据补记，未改实现、规格或历史FLOW。提交后仍保持Draft，最新远端CI另记。

## PostgreSQL 时区映射收口

`46d541d8170b3f11861614a9a43a9debfc65a015` 已提交推送，PR run `35747827910` 的wheel-smoke PASS；PG为58 PASS/2失败。两项失败均为真实Alembic check指出20个摄取时间列ORM默认无时区，0016/17迁移实际有时区。root按jobs既有模式显式声明DateTime(timezone=True)，不改迁移或现场数据。

冻结tree `a614b81abd8be6325a9bbd908afa1c1dba62ad4a` 独审BLOCKER0/BACKLOG0/REJECTED0。新回归在旧base真实RED（WITHOUT TIME ZONE与WITH TIME ZONE不等），在新tree逐项匹配全部20个时间列及nullable；store/artifact/metadata **36 passed / 3.56s**，Ruff及全量strict mypy **670文件PASS**。reviewer独立导出冻结tree复验36 PASS并认可本记录机械豁免。实际PG集成以该修复提交后的GitHub执行为准；未部署、未跑现场DDL或provider。
