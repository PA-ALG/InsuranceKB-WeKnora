# G3 D local UI implementation order amendment

Status: DESIGN_PENDING_INDEPENDENT_REVIEW; no UI write is dispatched by this file alone.

Applicable requirements: G3-R2/R3/R5. This changes only the local execution order in
lane-d-downstream-execution-plan-1.md, not the wire contract, owner matrix, acceptance criteria,
source approval, or delivery permissions.

The Python complete candidate and source-coverage2 fixture are frozen and independently PASS.
The Go public DTOs and positive complete candidate are frozen; independent review has also
confirmed all 354 repository snapshot payloads, scalar values and member digests equal the
Python projection. One Go nested omitted/null rejection defect is confirmed and awaits a
consolidated repair. That defect does not change the accepted wire shape or UI requirements.

## Bounded order change

After an independent review of this amendment passes, root may dispatch Task4 local UI
implementation in its existing exact owner paths using the frozen Python fixture, Catalog,
preparation/Active transport and alignment display contracts. The Go rejection repair and
review continue in the exclusive types lane. Backend Task1/2/3 remains closed until the
original Go entry gate passes.

The UI owner must implement strict required-key/nullable checks from the reviewed contract,
not copy Go's currently known permissive behavior. Use only existing local fixtures and
mocked transports. No HTTP service, browser runtime, source/provider request, build/image,
DB action, Draft, approval, activation or deployment is authorized by this order change.
No test may call a live server. Existing package installation prohibition remains.

The exact UI owner paths in the original plan remain unchanged. Python/C/common, fixtures,
Catalog implementation, router and KnowledgeBase.vue remain outside the UI write domain.
No DTO, route or source authority change is permitted. Any contract dependency discovered in
UI returns to root and is not solved by accepting partial objects or hardcoded scope data.

## Entry and completion gates

- Before dispatch: independent amendment review PASS; exact Python source-coverage2 freeze,
  Go DTO freeze, downstream plan and alignment clarification identities bound by root.
- UI work: meaningful mocked RED before implementation, focused GREEN and existing G2
  regressions/vue-tsc, then immutable source/test freeze for independent review.
- Root does not mark the combined D software complete, commit the UI as independently
  accepted, or integrate backend/UI until the final Go mirror review and Python-Go
  canonical/snapshot equality gates pass on the repaired exact identities.
- All original source, real C model, human review, build/deployment and live gates remain.

Reason: UI parser/rendering work depends on the frozen valid contract and fixtures; it does
not require a live backend or the Go invalid-input repair to be complete. The final integrated
acceptance still requires both sides to reject malformed input and agree on exact content.
