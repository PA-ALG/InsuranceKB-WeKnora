# G2 isolated release prerequisites — read-only audit

Review/publish status: NOT_RUN. Actual A compiler and independent reviewer outputs now exist; the v2 candidate is frozen in a-human-candidate-preflight.json. The actual Draft result is recorded separately in a-human-draft-create.json when executed. This record grants no review or publish approval.

The current isolated configuration contains only the citation-read ring. Human-decision and publish-authorization public-key rings are empty and therefore fail closed. The user’s standing G2 budget/continuation approval covers bounded isolated configuration and API operations; it is not evidence that a named human reviewed a particular future candidate, and it must not be converted into a fabricated human decision or signature. Independent LLM review is a separate fact.

After an actual candidate exists, bind its complete bundle/hash, compiler and independent reviewer raw outputs, existing67 field facts, proposed concept/navigation changes, original PDF and native-source identities for real human review. Provision separately authorized public-only Ed25519 entries under the existing human-decision and publish rings; preserve the citation ring and keep private keys outside the repository/server config. Use the existing canonical signing functions and separate signer domains.

API sequence under `/api/v1/knowledgebase/{wiki}/wiki/release-scopes/{space}/raw/{raw}`:

1. GET `/current` and bind the actual release/epoch.
2. POST `/schema/preparations` with exactly preparation_id and concept_candidate_bundle. Capture Draft candidate, ready-receipt, policy, manifest and preparation digests.
3. Following actual named-human review, POST `/schema/preparations/{id}/review` with the valid closed HumanBatchDecisionReceiptV1. Its canonical web principal must equal the same authenticated administrator used by the call. Source authority is independently replayed before Ready.
4. After Ready and valid independent publish authorization, POST `/activations` with that same human receipt and PublishAuthorizationV0 bound to all Ready digests, scope, expected Head and nonce. Source/ACL/signature/CAS checks run again before atomic activation.
5. Compare activation receipt to `/current`, require epoch=old+1, then pin that exact release for pages/source/search/Agent probes. Repeat the authorized sequence for the second real entity-set snapshot. Never infer FLOW from fixtures, code tests, or container health.

Current G2 routes do not provide a browser draft concept/citation preview. Do not claim browser-based prepublication source review; frozen Draft bundle, original PDF/SourceRevision and server verifier evidence are the current review materials. No keys, approvals or signatures were generated by this read-only audit.

Deployment prerequisite: after recreating the app and confirming health200, reload the exact isolated UI nginx so its upstream hostname resolves the new container IP, then recheck proxy login/tenant10003. UI/data/runtime source identities remain separately recorded.
