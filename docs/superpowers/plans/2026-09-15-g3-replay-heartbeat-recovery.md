# G3 replay heartbeat and repeated recovery

Existing user-authorized G3-AUTO-3/4/6. Root sole writer; existing extraction lane supplies bounded patches and existing reviewer independently checks frozen code and delivery. Same environment/database/models. No real business continuation scripts.

Task3am source0ae8d97e deployed successfully, but webpage recovery b8a6ce3f-3931-59c1-8eea-53f3a7d0a947 (clicked21:56:54.988Z) failed identity after three lease_expired attempts; last job finished22:03:18.644462Z, root finished22:03:20.7271Z. Source succeeded on attempt2; routing succeeded. No new provider call or field plan/extraction/publication. Five-minute resource sample had no OOM/restarts and worker peak2.412GiB fell to595MiB. Compact field-input software is deployed but untested on this live run because recovery did not reach it.

Confirmed code path: replay_stage_call has no awaits; get/record replay and prepare_artifact_writes acquire the WikiJob row lock before repeatedly loading and hashing both origin/current large source snapshots. Event-loop blocking and holding the heartbeat's row lock across these reads can expire the lease. Metrics also repeat full replay validation. Do not merely raise the lease or remove integrity checks.

1. RED for heartbeat progress while slow replay source validation is in flight, ordering of source validation before the job fence, reuse across multiple replay artifact drafts, tampered sources/calls and stale generations. Keep existing recorded v1 request and processing v3 plan contracts compatible.
2. Run blocking replay storage and artifact preparation in worker threads. Validate complete source payloads before acquiring the short WikiJob fence, holding consistent shared locks on the bound immutable source/plan/call rows through the transaction. Reuse one validation per preparation transaction; no per-draft large rereads. Final fenced commit still rejects stale generations. Do not dispatch a new model call.
   Terminal task GET also exceeded30seconds: historical replay call metrics must read only recorded plan/call identities and usage, not load all source payloads again. Keep scope/recorded-call binding checks; this lightweight observational path never authorizes extraction or publication and must not replace their full validation.
   Likewise, GET/list capability hints inspect source identity/digest columns only. The actual retry action and worker keep full payload hashing; a displayed retry button is not a guarantee that a damaged checkpoint will be admitted. Move source reuse/read/decode and routing snapshot reads off the event loop as well; the live source job already expired once.
3. Permit explicit platform recovery of a failed recorded-identity replay before windows/fields exist. Reuse the original ancestor call only through a verified processing-recovery ancestry; bind exact original call digest, scope, source snapshots and immediate parent version. Repeated recovery remains idempotent, preserves prior terminal runs, rejects cycles/tampering/foreign calls, and never causes reclassification.
4. Bounded write domains: product_ingestion artifacts.py, model_execution.py, stages.py, store.py, recovery.py only if needed, plus their focused tests and this evidence/plan. No schema migration or model/runtime limit changes. Avoid a new recovery contract where existing v3 already binds the original call digest.
5. Local synthetic tests and independent review before one existing Harness build/smoke/deploy. All runs must be terminal. Preserve Task3am image/runtime env for rollback. Actual recovery only through the webpage; source/PDF/evidence/field results must never be assembled or altered by Codex.

Fresh acceptance, 5–10-minute publishing and G3 completion remain unproven. Record real recovery wall time including queues/retries, not only the last successful stage attempt.
