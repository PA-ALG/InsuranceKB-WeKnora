# G3 field worker heartbeat closure

Existing user-authorized G3-AUTO-2/3/4/6. Root remains sole repository writer; existing extraction lane supplies private fixture tests/patches and existing reviewer checks frozen changes. Reuse current services, database, models and runtime limits. No manual business continuation.

Task3an source `5e62d5f98ede9b69e62a821e2df442148ff60e92` is frozen in the deployed runtime. Web run `62073d58-d3e4-5371-9290-38da2eab35ef` started 2026-09-14T22:43:28.091921Z after a 57.41-second admission delay. Source, routing, identity replay and field planning succeeded on attempt 1. Identity took180.39s and field planning73.82s. Nine extraction windows were scheduled. The first four windows remained reserved with zero provider dispatches while job attempts advanced on lease expiry. This is a real remaining platform failure; Task3an publication acceptance is not PASS. Keep its image unchanged and observe its final state before any replacement.

Confirmed code gaps: async `stages.load_source_blocks` contains no await and synchronously reads/decodes all source snapshots per window. `worker` task restoration and configured request preparation, and `extraction.execute_window` source indexing/rendering/projection remain synchronous. `store.reserve_window` locks WikiJob before hydrating/comparing the full ProductWindow.tasks JSON. Existing Task3an tests did not exercise the production field loader under this load.

1. RED in a private fixture against the frozen source for event-loop progress while the production field loader reads/decodes, request rendering/projection is blocked, and large window task hydration occurs before the job fence. Retain generation expiry, scope/digest mismatch, v1/v2 strict replay and uncertain-call no-redispatch controls. Environment/setup errors do not count as RED.
2. Offload blocking source decode and pure task/request/result computations with `asyncio.to_thread`; retain provider HTTP and begin/persist callbacks on the original async loop. Never run a second event loop around provider clients. Do not loosen source/evidence/model limits. Avoid unrestricted concurrent large source hydration; if needed use a scoped async loader gate, released only when the underlying read finishes even if its waiter is cancelled. Do not introduce a cross-run cache or trust digests instead of full validation in this patch.
3. Move full task hydration/comparison before the short WikiJob fence. Audit every same-window writer and preserve a consistent row-lock order, complete task comparison, immutable selection, exact call identities and stale-generation rejection. Do not create a window/call lock inversion with begin/record. Window responses are bounded and should not be conflated with the134MB source snapshot; change only demonstrated slow lock boundaries.
4. Bounded paths: product_ingestion stages.py, worker.py, extraction.py, store.py and composition.py only if a scoped loader gate is needed; focused tests and plan/evidence. No migrations, new services, resource/model/lease-limit changes, or business mutations. Runtime/UI admission-response latency remains a separately recorded defect; no hidden retries to disguise it.
   Root also closes native recovery admission for this exact infrastructure failure: a terminal failed aggregate extraction stage with every window terminal, no dispatched/recorded field call and no field attempt may reuse the existing v3 recorded-identity plan. Validate all window/job/call scope bindings and original source/identity provenance; reject active windows, any uncertain or recorded dispatch, results, or mismatched calls. Do not infer permission to replay a partly completed extraction from this exception. The previous terminal run is immutable. This is needed because the current platform rejects every processing retry once any window row exists, including wholly undispatched failed work. No new contract or direct business operation is introduced.
5. Root applies validated patches after live Task3an results are recorded, then focused compatibility tests, frozen independent review, one existing Harness build/smoke/deploy after all tasks are terminal. Webpage-only recovery must preserve successful prior parse/classification/extraction results. No fixture output can substitute for real publication and evidence readback. Ordinary field failures must remain visible and non-blocking; do not repeatedly call a model merely to fill them.

Task3an and future recovery times include request admission, queues, retries and all stages. Fresh-product acceptance, performance target and G3 completion remain unproven.
