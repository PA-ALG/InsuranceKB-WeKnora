# G3 native geometry memory recovery

Existing G3-AUTO-3/4/6, same G3 environment and user authorization apply. Root sole repository writer/deployment owner; existing g3_extraction_finish supplies test and implementation patches only; g3_incremental_code_review independently reviews frozen patch. No new product feature, metadata contract, model call or database.

## RED and scope

128MiB transport configuration allowed the91.65MB response. At2GiB, APP0313 processed77parent/1372childchunks at19:59:47.240Z, then OOMKilled/exit137 at20:00:15.002419823Z. No further resource-limit increases. Prior independent run and both recovery failures remain failed. Actual maximum allocation site remains unprofiled; confirmed code paths repeatedly decode and canonicalize complete per-character geometry with generic map trees.

## Smallest compatible implementation

Bounded owner paths: internal/application/service/concept_source_authority_830_g2.go, knowledge_process.go, g3_first_parse.go, internal/infrastructure/docparser/grpc_parser.go and same-package focused tests. Only add narrowly scoped helper files if justified. Do not redesign transport/snapshot contracts or alter frozen published data.

1. RED: generated native fixture demonstrates excessive allocations in current canonical projection encoding; use existing fixture hashes plus legacy canonical encoding as exact oracle. No real PDF processing/provider/database required. Add tamper/unknown/malformed/coordinate/hash regressions so faster code cannot skip validation.
2. Native-specific canonical encoding must emit exactly existing bytes for valid projection/parser/page/bbox structures without round-tripping through `map[string]any`. Other generic canonical consumers remain unchanged. Avoid allocating a second million-bbox projection solely to read the parser identity; use a header projection while retaining existing full strict validator.
3. Investigate releasing the duplicate native envelope from transient metadata only after validation and only if all raw evidence/hash identities remain represented and no supported consumer depends on that key. Preserve immutable evidence, all character coordinates, strict binding and existing on-disk bytes. If uncertain, defer this part.
4. Do not add a new global validation cache or disable checks. Request-scoped reuse must not introduce mutable aliases. No broad service refactor or byte-budget cache redesign in this repair.
5. Compare synthetic allocation/peak-memory measurements and exact canonical bytes/hashes before/after; focused package tests and independent review. Documentation-only evidence corrections do not require new RED.
6. Freeze source before existing APP build/smoke/deployment, retain original rollback assets and same2GiB runtime. Build only to deploy this code repair; ordinary uploads never build.
7. Restore platform availability and use webpage cancellation/reparse controls only for the interrupted material if needed; no Redis/DB job mutations or custom business continuation. Subsequent recovery is not fresh acceptance.

## Deferred architecture

Chunked native-artifact transport, page-selective signed snapshots and byte-bounded resident caches remain explicit follow-up design needs. This scoped repair must materially reduce current peak while preserving v1 evidence semantics; if it cannot, do not claim G3 pass or keep increasing memory.

## Availability recovery while implementation is tested

An attempt to restore APP0313 for webpage cancellation also resumed the queued parser before the UI operation could complete; it OOM-exited again20:09:38.682697602Z with the same2GiB cap. No cancellation was submitted. Do not restart this known memory-failing configuration again before code repair.

Restore the retained pre-Task3ah APP container3decc7fbd81527436b574c409e4e8fe5e1a0108468697f6b223dbfc82098862e to its original service name, using its original1GiB/50MiB client cap and exactsame d312 image. Rename the stopped0313 container for later forensic/rollback use. DocReader remains128MiB; large responses will be rejected by the restored client's50MiB cap instead of materializing the full native payload. This is temporary availability rollback, not a business fix. Original source and task evidence remain unchanged; queue alone finalizes any remaining attempt. No new image/volume/DB/credential or direct business mutation. Verify health and originalservinghead, then deploy the reviewed memory repair with128MiB/2GiB when ready.

## Software result and bounded build preparation

Root confirmed actual RED49381allocations versus20980limit; GREEN86allocations. All42focused top-level tests(72including subtests)PASS,0failed.50k-character preparation benchmark falls96,616,992→37,188,000allocated bytes/op and1,200,420→250allocations/op; bytes allocated are not peakRSS. Independent source review0BLOCKER. Rawmetadata removal deferred; exactly3productionservicefiles change,plus one test.

Reuse successful Task3ae/af02 APP build/smoke scripts, replacing only outputnamespace/contract, exact runtimebase with currentd312/source931, and prior-successreceipt provenance. No cold/full-cache claim. One binary-build invocation, existingBuildKitcache/dependencylayers, no pull; runtimeCOPYbinarylayer only. ExistingVM disk admission4.5GiB and stopreserve1GiB; host admission1GiB/reserve512MiB. Prior successful build used about1.2GiB netadditionaldisk, but warm-cache/fit is not guaranteed. Bound this incremental build to1800seconds; on failure preserve receipt and stop. No resize/prune in build script. Current software must be frozen clean before invocation. Script/smoke independent review before execution. Deployment will retain128MiBclient and2GiBresource configuration, with no newservices/database.
