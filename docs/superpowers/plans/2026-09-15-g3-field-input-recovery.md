# G3 compact field inputs and platform recovery

Existing G3-AUTO-2/3/4/6 and user authorization apply. Root is sole repository writer and deployment owner; existing extraction lane supplies patches and existing reviewer checks frozen changes. Reuse the existing application, database, model configuration and source evidence.

Real recovery d9ae11aa-642b-5519-95fc-24c6847b52dc ended failed at 2026-09-14T21:31:25.796682Z, 384.51 seconds after its webpage retry. Source, routing and identity succeeded; field_plan failed after three attempts with configured model context capacity exceeded. No field extraction or publication occurred. The model targets repeat all approximately 1401 allowed-source records for every field even though offered text is routed and bounded.

1. RED with synthetic large source inventories: even a single field exceeds the current request budget. Keep full local FieldTask, dependency hash and evidence validation; use a versioned compact model target without repeated full source inventory. Strictly validate both recorded v1 and new v2 requests. Do not raise model limits or truncate unrecorded evidence.
2. Deterministic field-plan policy failures must reach a terminal job state without repeating the identical local calculation. Tests distinguish capacity from unrelated policy failures.
3. Extend existing recorded-identity processing recovery to failed field planning only before windows/field attempts exist, with succeeded source/routing/identity stages and intact source snapshots and recorded identity. Replay original identity request/response under existing drift checks; never redispatch classification or recapture completed source bodies. Preserve origin terminal history and idempotent child identity.
4. Test the real worker path using synthetic platform/model boundaries: failed plan, native recovery, persisted parse/identity reuse, extraction and publication. No real business requests during code testing. Existing UI capability exposes recovery through its current button; precise wording can follow without a separate UI rebuild.
5. Focused tests and independent review precede a single existing Harness build/smoke/deployment. Preserve Task3al runtime configuration and rollback image; all runs must be terminal before replacing API/worker. No new DB, migration, credentials or APP build.
6. Use the webpage recovery control and observe platform execution. Codex cannot assemble inputs, field outputs, candidates or publish scripts. Report actual stages/calls/evidence and retain failed original/fresh acceptance; recovery cannot establish fresh-upload acceptance or a 5–10 minute target in advance.
