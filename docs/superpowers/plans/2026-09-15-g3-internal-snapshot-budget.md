# G3 internal source snapshot budget

Existing user-authorized G3-AUTO-3/4/6 scope, existing environment and service accounts. Root owns edits/deployment; existing reviewer independently checks code and scripts. No new database or business continuation scripts.

Task3aj rune reuse passed software tests but APP still OOM-exited21:07:04Z. One same-image configuration recovery added GOMEMLIMIT1400MiB and GC diagnostics while keeping the2GiB container cap. Source snapshot completed in84.995s. The subsequent job failed with PLATFORM_RESPONSE_LIMIT_EXCEEDED at21:13:14Z and the product entered failed at21:13:15Z. Its actual internal response budget is64MiB. The fee native geometry is91.65MB before base64 encoding, so it cannot fit. This transfer is between local services, not model context.

1. Extend PlatformConnectionSettings' finite allowable response cap to192MiB. Keep streamed overflow rejection, source signatures and all model request/response/window settings unchanged.
2. RED:128/192MiB configuration rejected by old64MiB cap; zero, negative and above192MiB still rejected. GREEN:23 configuration/client/budget tests passed.
3. Package existing Harness source using pinned/cache layers; deploy the existing API/worker only. New private runtime env differs solely at bindings[0].platform.max_response_bytes64→192MiB. Preserve the old compose/env for rollback, database and migrations unchanged.
4. Before deployment require all product runs terminal. Then use webpage recovery for child9c408866-0684-532c-ba01-8b08dd1c752d, reusing three successful source parses and71 recorded source model calls. No new parse, fields, candidates or release assembled by Codex.

GC observation21:12:59–21:17:49Z:30samples, no OOM/restarts/max events; APP kernel lifetime peak1.912GiB and ending1.078GiB, final GC live heap545MB. This proves only that observed snapshot attempt completed without OOM, not overall stability or G3 completion. Large inline source snapshots, cancellation and stage-level reuse remain architectural follow-ups. No fresh-upload or5–10minute publication PASS claim.
