# PR #13 Takeover Implementation Plan

> Execute test-first. Do not merge until all local gates and GitHub checks pass.

## 1. Establish the synchronized baseline

- Merge the exact PR #13 head with current `origin/main` in an isolated worktree.
- Resolve only semantic conflicts and run ruff, mypy, and deterministic pytest.
- Preserve the clean baseline result in the validation report.

## 2. Add durable-ledger failing tests

- Add `test_recall_attempt_ledger_024.py` covering durable reservation, hard
  budget after reopen, uniqueness, outbound failure, field linkage, and assignment.
- Extend extraction, gapfill, vote, judge, and finalize audit tests for producer
  correctness and complete history.
- Run the focused tests and capture the expected RED failures.

## 3. Implement the durable attempt boundary

- Add `compiler/attempts.py` with SQLite and in-memory ledger implementations.
- Reserve before outbound calls and finish with explicit outcomes.
- Return the producing attempt from parse helpers.
- Inject the run ledger into extract, gapfill, vote, and judge paths.
- Query the ledger during finalize; keep state counters and candidate metadata only
  as compatibility projections.

## 4. Add fail-closed configuration tests and implementation

- Test unknown Pydantic keys, blank/trimmed experiment IDs, requiredness alias
  conflicts, nonnegative gapfill CLI limits, and propagation of experiment flags.
- Forbid unknown keys and add canonical validators.
- Add shared CLI arguments and a single config builder for both extract commands.

## 5. Reconcile contracts and documentation

- Update OpenSpec 024 proposal/tasks/spec/validation report to describe the actual
  ledger, remaining 020 D4b work, and current evidence.
- Update HANDOFF and the PR description; remove superseded claims instead of
  appending contradictory notes.
- Run strict OpenSpec validation if available.

## 6. Verify and integrate

- Run focused recall tests, full ruff, full mypy, full deterministic pytest, and
  repository-specific smoke/contract checks.
- Review the final diff against `origin/main`, commit, and push to the PR #13 head.
- Wait for GitHub checks. Merge only when required checks pass and no unresolved
  blocking finding remains.
- Refresh `origin/main` and verify the merge commit/check status.
